<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ════════════ Bluetooth Permissions ════════════
         Galaxy A20 (Android 9/10): BLUETOOTH + BLUETOOTH_ADMIN + FINE_LOCATION.
         Android 12+: BLUETOOTH_CONNECT/SCAN (mit usesPermissionFlags neverForLocation).
    -->
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30"/>
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"/>
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"
        android:maxSdkVersion="30"/>

    <!-- Cockpit-Constraints -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.VIBRATE"/>

    <!-- Wird gebraucht, sobald MQTT-Relay implementiert wird -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <uses-feature
        android:name="android.hardware.bluetooth"
        android:required="true"/>

    <application
        android:name=".App"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@android:drawable/sym_def_app_icon"
        android:theme="@style/Theme.Thaiger"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:hardwareAccelerated="true"
        android:supportsRtl="true">

        <!-- Screen 1: Car Select -->
        <activity
            android:name=".ui.CarSelectActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Screen 2: Connecting -->
        <activity
            android:name=".ui.ConnectingActivity"
            android:exported="false"
            android:screenOrientation="portrait"/>

        <!-- Screen 3: Dashboard -->
        <activity
            android:name=".ui.DashboardActivity"
            android:exported="false"
            android:screenOrientation="landscape"
            android:keepScreenOn="true"/>

        <!-- Screen 4: Alert Overlay -->
        <activity
            android:name=".ui.AlertActivity"
            android:exported="false"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.Thaiger.FullScreen"/>

        <!-- Screen 5: Post-Run Summary -->
        <activity
            android:name=".ui.PostRunActivity"
            android:exported="false"
            android:screenOrientation="portrait"/>

        <!-- Screen 6: Settings -->
        <activity
            android:name=".ui.SettingsActivity"
            android:exported="false"
            android:screenOrientation="portrait"/>

        <!-- Screen 6b: MQTT-Broker-Untermenü -->
        <activity
            android:name=".ui.MqttSettingsActivity"
            android:exported="false"
            android:screenOrientation="portrait"/>

    </application>
</manifest>