<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base app theme — dark, no action bar, status bar matches cockpit bg -->
    <style name="Theme.Thaiger" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@color/bg_primary</item>
        <item name="android:statusBarColor">@color/bg_primary</item>
        <item name="android:navigationBarColor">@color/bg_primary</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="colorPrimary">@color/blue</item>
        <item name="colorAccent">@color/blue</item>
    </style>

    <!-- Fullscreen variant for the alert overlay -->
    <style name="Theme.Thaiger.FullScreen" parent="Theme.Thaiger">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>

</resources>
