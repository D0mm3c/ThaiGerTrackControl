# ThaiGer Track Control — 使用手册（中文）

> 面向 **ThaiGer H₂** 氢燃料电池赛车（**Thaiger 7** 与 **Bengalo**）的遥测座舱系统。
> [🇬🇧 English](GUIDEBOOK.en.md) · [🇩🇪 Deutsch](GUIDEBOOK.de.md) · 🇨🇳 中文

---

## 目录

1. [本项目是什么](#1-本项目是什么)
2. [系统总览](#2-系统总览)
3. [第一部分 — 安卓 App（车手 / 车辆）](#3-第一部分--安卓-app)
   - [A.1 安装与首次启动](#a1-安装与首次启动)
   - [A.2 权限](#a2-权限)
   - [A.3 界面 1 — 车辆选择](#a3-界面-1--车辆选择)
   - [A.4 界面 2 — 连接（蓝牙 / 演示模式）](#a4-界面-2--连接)
   - [A.5 界面 3 — 实时仪表盘](#a5-界面-3--实时仪表盘)
   - [A.6 界面 4 — 赛后总结与 CSV 导出](#a6-界面-4--赛后总结与-csv-导出)
   - [A.7 界面 5 — 设置](#a7-界面-5--设置)
   - [A.8 界面 6 — MQTT 设置](#a8-界面-6--mqtt-设置)
4. [第二部分 — 工程师仪表盘（笔记本电脑）](#4-第二部分--工程师仪表盘)
   - [B.1 打开与连接](#b1-打开与连接)
   - [B.2 视图 1 — Operations 运营总览](#b2-视图-1--operations-运营总览)
   - [B.3 视图 2 — Live Tracking 实时追踪与地图](#b3-视图-2--live-tracking-实时追踪与地图)
   - [B.4 视图 3 — Analytics 分析](#b4-视图-3--analytics-分析)
5. [遥测协议与字段参考](#5-遥测协议与字段参考)
6. [MQTT 主题与负载](#6-mqtt-主题与负载)
7. [车辆配置档](#7-车辆配置档)
8. [故障排查](#8-故障排查)
9. [调参与高级设置](#9-调参与高级设置)
10. [术语表](#10-术语表)

---

## 1. 本项目是什么

ThaiGer Track Control 是一套面向氢燃料电池赛车的两端式遥测系统：

- 一个运行在**车内手机**上的 **安卓 App**。它通过**低功耗蓝牙（BLE）**接收赛车控制器
  的实时遥测数据，在赛车仪表盘上展示给车手，记录本次行驶，并通过 **MQTT** 经互联网
  **转发**数据。
- 一个 **工程师仪表盘** —— 一个单独的 HTML 文件（`engineer_dashboard.html`）——
  运行在维修区**工程师笔记本电脑**的浏览器中。它订阅同一条 MQTT 数据流，显示运营数据、
  带功率着色 GPS 轨迹的实时地图，以及分析图表。

两端从不直接通信；它们在云端的 **MQTT 代理服务器（broker）** 处汇合。

---

## 2. 系统总览

```
   ┌──────────────┐   BLE    ┌────────────────────┐   MQTT/TLS    ┌──────────────────┐
   │  赛车         │ ───────▶ │  安卓 App           │ ───────────▶ │  MQTT 代理        │
   │  控制器       │  *A28.3* │  (车内手机)         │  JSON 帧     │  (HiveMQ Cloud)  │
   └──────────────┘          └────────────────────┘               └────────┬─────────┘
                                      │                                      │ WebSocket/TLS
                                      │ GPS（手机 LocationManager）          ▼
                                      ▼                             ┌──────────────────┐
                               thaiger/{car}/gps                    │ 工程师            │
                               thaiger/{car}/telemetry              │ 仪表盘 (HTML)     │
                                                                    └──────────────────┘
```

- 每辆车有**两个 MQTT 主题**：`thaiger/{car}/telemetry` 与 `thaiger/{car}/gps`。
- `{car}` 是车辆 ID：**`thaiger7`** 或 **`bengalo`**。
- 手机负责发布（publish），工程师仪表盘负责订阅（subscribe）。任意数量的笔记本电脑可
  同时观看同一辆车。

---

## 3. 第一部分 — 安卓 App

### A.1 安装与首次启动

本 App 是标准的安卓项目（Gradle）。构建调试版 APK：

```bash
./gradlew assembleDebug
```

要求：

| 项目 | 取值 |
|---|---|
| 最低安卓版本 | 9.0（API 28） |
| 目标安卓版本 | 14（API 34） |
| 语言 | Java 11 |
| 应用 ID | `com.thaiger.h2racing` |

将生成的 APK 安装到将放置在车内的手机上。首次启动时，App 打开 **车辆选择** 界面。

> **提示：** 以横屏方式将手机固定在车手能看到的位置。行驶过程中仪表盘会保持屏幕常亮
> （见 设置 → *保持屏幕常亮*）。

### A.2 权限

App 在使用任何硬件之前，会在 **连接** 界面请求权限。请全部授予以获得完整功能：

| 权限 | 用途 |
|---|---|
| 蓝牙连接 / 扫描（安卓 12+） | 配对并与车辆 BLE 模块通信 |
| 蓝牙 / 蓝牙管理（安卓 ≤ 11） | 同上，用于较旧安卓 |
| **定位（精确）** | 安卓要求其用于 BLE 扫描**以及** GPS |
| 定位（粗略） | GPS 回退 |
| 互联网 | 向工程师笔记本电脑做 MQTT 转发 |
| Wake Lock | 行驶中保持屏幕常亮 |
| 振动 | 报警时的触感反馈 |

若拒绝蓝牙权限，你仍可使用 **演示模式**（合成数据）来体验 App。

### A.3 界面 1 — 车辆选择

- 两张卡片：**Thaiger 7** 与 **Bengalo**。点按卡片即可选择（蓝色描边标记当前选择）。
  默认选中 Thaiger 7。
- 所选配置档决定报警阈值与最大功率刻度。
- 点按顶部 **Settings**，在行驶前打开 App 设置。
- 点按 **Connect to …** 进入连接界面。

### A.4 界面 2 — 连接

此界面把手机与赛车配对并启动数据流。

1. App 检查权限以及蓝牙是否开启。
2. 弹出对话框，列出你**已配对的蓝牙设备**，并在末尾附上一项 **⚙ 演示模式**。
3. 选择你赛车的模块（例如名称含 HM-10 / HC-05 / ESP32 的设备），**或**选择 **演示模式**
   以在无硬件的情况下运行合成数据流。
4. **Pair Neu / 配对新设备** 会打开安卓蓝牙设置（若模块尚未配对）。**Cancel** 返回车辆选择。

当模块报告 **Connected（已连接）** 后，App 会**自动跳转**到实时仪表盘。若在设置中启用了
MQTT 转发，它也会在此启动——因此一旦行驶开始，工程师笔记本电脑便开始接收数据。

> 连接中断会以指数退避自动处理（1 → 2 → 4 → 8 → 16 秒）。你会看到 *Reconnecting…*，
> 无需任何操作。

### A.5 界面 3 — 实时仪表盘

仪表盘是车手的主视图（横屏，屏幕保持常亮）。它持续刷新，显示每个遥测字段的最新值。

**关键元素**

- **速度** —— 大号主数字（km/h）。若开启了 *速度颜色警告*（设置），当你低于该车的最低
  目标速度时数字变为**红色**，否则保持浅色。一个小指示器显示 *✓ on target（达标）* /
  *✗ too slow（过慢）*。
- **圈速浮层**（底部栏上方的一行）：`PREV 1:18 · LAP 3 · CURR 0:09` —— 上一圈用时、
  当前圈号，以及当前圈的实时计时。
- **电气区块** —— 燃料电池 / 超级电容 / 电机的电压与电流、自身消耗、电芯电压差。
- **热与效率** —— 燃料电池温度、空气泵占空比、燃料电池与系统效率、能量、最优速度、
  驾驶提示。
- **报警横幅** —— 当某阈值被越过时出现（例如燃料电池温度或电芯电压差过高）。开启
  *振动* / *报警声* 后还会有触感 / 声音反馈。点按 **Dismiss** 隐藏横幅。

**结束一次行驶：** **长按速度数字。** 这会停止蓝牙与 MQTT 转发，结算本次行驶统计，并
打开 **赛后总结**。

### A.6 界面 4 — 赛后总结与 CSV 导出

行驶结束后你会看到汇总结果：

- 行驶时长、距离、平均速度、消耗能量、电机峰值功率、燃料电池最高温度，以及报警次数。
- 整段行驶的 **功率曲线图**。

**按钮**

- **Export CSV（导出 CSV）** —— 将每一帧已记录的遥测数据写入 CSV 文件，并打开安卓分享
  面板，便于通过邮件、网盘、即时通讯等发送。文件名为
  `thaiger_{car}_{YYYYMMDD_HHMM}.csv`，包含 21 列（时间、速度、各路电压、各路电流、
  温度、能量、效率、距离、电机功率）。若未记录到数据，会提示 *“No data to export”*。
- **New Run（新行驶）** —— 直接返回连接界面开始下一次行驶。
- **Back to Car Select（返回车辆选择）** —— 回到起点。

### A.7 界面 5 — 设置

通过车辆选择界面的 **Settings** 链接进入。所有取值立即保存。

**显示**

| 设置 | 默认 | 含义 |
|---|---|---|
| 保持屏幕常亮 | 开 | 持有 wake lock，使行驶过程中屏幕不休眠 |
| 速度颜色警告 | 开 | 低于该车最低目标速度时把速度数字变红 |
| 刷新频率 | 50 ms | 仪表盘 UI 的重绘间隔（20–500 ms） |

**报警**

| 设置 | 默认 | 含义 |
|---|---|---|
| 振动 | 开 | 报警触发时的触感脉冲 |
| 报警声 | 关 | 报警触发时播放提示音 |

**阈值（每辆车独立）** —— 为 Thaiger 7 与 Bengalo 分别保存：

| 设置 | 范围 | 默认（Thaiger 7 / Bengalo） |
|---|---|---|
| 燃料电池温度上限 | 30–120 °C | 70 °C / 65 °C |
| 电芯电压差上限 | 10–500 mV | 50 mV / 50 mV |

越过阈值即触发仪表盘报警。

### A.8 界面 6 — MQTT 设置

设置的一个子界面，用于配置到工程师笔记本电脑的转发。仅当在开始行驶**之前**启用，转发
才会运行。

| 字段 | 默认 | 说明 |
|---|---|---|
| 启用转发 | 关 | 总开关 —— 打开后才会发布遥测 |
| 使用 TLS | 开 | 加密连接（推荐；HiveMQ Cloud 要求） |
| Broker 主机 | — | 例如 `abc123.s1.eu.hivemq.cloud` |
| 端口 | 8883 | TLS MQTT 端口（通常 8883；明文用 1883） |
| 用户名 | — | Broker 用户名 |
| 密码 | — | Broker 密码（以掩码显示） |
| 发布频率 | 5 Hz | 遥测发布频率，1–10 Hz |

> 手机通过**原生 MQTT（TLS）**发布。工程师仪表盘通过 **MQTT over WebSocket** 连接到
> 同一个 broker —— 因此 broker 还必须暴露一个 WebSocket 监听端口（HiveMQ Cloud 提供，
> 端口为 8884）。见第二部分。

---

## 4. 第二部分 — 工程师仪表盘

`engineer_dashboard.html` 是一个**单一、自包含的文件**。它无需构建步骤、无需服务器
—— 只要一个现代浏览器与互联网连接（用于地图瓦片、字体，以及来自 CDN 的 MQTT / 图表
库）。

### B.1 打开与连接

1. 在 Chrome/Edge/Firefox 中打开 `engineer_dashboard.html`（双击，或托管到任意静态 Web
   服务器）。
2. 首次使用时 **Connection Settings（连接设置）** 对话框会自动弹出（或点击右上角的
   **⚙** 按钮）。
3. 填写：
   - **Car** —— `Thaiger 7` 或 `Bengalo`（必须与手机发布时所用的车辆一致）。
   - **Broker URL (WebSocket)** —— 完整的 WebSocket URL，例如
     `wss://abc123.s1.eu.hivemq.cloud:8884/mqtt`。TLS 用 `wss://`（HiveMQ Cloud 为端口
     8884），本地网络的明文 broker 用 `ws://host:port`。
   - **Username** / **Password** —— broker 凭据。
4. 点击 **Connect**。设置会保存在浏览器中（`localStorage`），下次打开仪表盘会自动重连。

右上角的状态圆点表示连接：**灰色** 未连接、**琥珀色** 连接中、**绿色** 已连接。其旁显示
车辆标识与最近一帧的接收时间。

顶部三个标签页在视图间切换：**Operations**、**Live Tracking**、**Analytics**。

### B.2 视图 1 — Operations 运营总览

一眼总览整车状态：

- 一张 **主速度卡片**（目标感知：达到/高于最优速度时为蓝色，低于则为红色），含平均速度、
  圈数、行驶时间、目标圈速与距离。
- 一个 **电气** 网格：燃料电池 / 超级电容 / 电机的电压与电流、自身消耗、电芯电压差
  —— 每项均配有上下文进度条。
- 一个 **热与效率** 网格：燃料电池温度、空气泵占空比、燃料电池与系统效率、能量、最优
  速度、驾驶提示。
- 一个跳动的 **LIVE** 标记，确认正在接收新鲜数据。

### B.3 视图 2 — Live Tracking 实时追踪与地图

一张全屏暗色 **OpenStreetMap**（Leaflet），跟随赛车。

- **功率着色轨迹** —— 车后的路径按段绘制，并按该时刻的电机功率着色：
  **绿色（0 W）→ 琥珀（100 W）→ 红色（200 W+）**。轨迹最新的部分完全不透明，随时间
  逐渐淡出 —— 因此你既能读出赛车*去过哪里*，也能读出它在每个点上*出力多大*。
  （200 W 为 ThaiGer 追踪时的最大功率；刻度常量 `POWER_MAX` 可在文件中修改。）
- **功率图例**（左下角）—— 颜色刻度加上实时瓦数读数。
- **旋转车辆标记** 指向行驶方向。
- **GPS 状态面板**（右侧）—— 定位质量、精度、纬度、经度、高度、GPS 速度、航向、实时
  电机功率、圈数，以及一个 **Filtered** 计数器（被剔除的噪声 GPS 定位数量 —— 见下文）。

**GPS 抖动清理。** 原始 GPS 可能乱跳。每个定位在进入地图前都会经过四步清理：
（1）剔除精度差于 75 m 的定位；（2）拒绝“瞬移” —— 隐含速度不可能（> 约 162 km/h）的
定位，并带有重新同步保护，使其永不卡死；（3）静止时忽略小于 1.5 m 的抖动；（4）对残余
噪声做轻度平滑。最终得到无随机跳变的干净轨迹。

**每圈快照与重置。** 每当赛车圈数计数器递增时，仪表盘会自动**把刚结束那一圈的轨迹渲染
为 PNG 图片并下载**（`thaiger-lapN-<时间戳>.png`），随后**清空轨迹**，使新的一圈从头
绘制。每次保存都会有一个绿色提示条确认。

### B.4 视图 3 — Analytics 分析

- 一张 **实时遥测表格**，列出全部 22 个字段及其当前值。
- 一张 **Speed（速度）** 图（km/h 随时间）与一张 **Efficiency（效率）** 图（燃料电池
  与系统效率随时间），各显示最近约 60 个采样点。

---

## 5. 遥测协议与字段参考

赛车控制器通过 BLE 发送 ASCII 帧。每个字段以星号包裹，并以单个字母作为键。示例：

```
*A28.3**B25.0**C3**D15:35**N53.4*
```

解析器对数据流容错：能处理部分包、多块帧与缺失字段，并始终保留每个字段的最近已知值。

| 键 | 字段 | 单位 |
|---|---|---|
| A | 速度 | km/h |
| B | 平均速度 | km/h |
| C | 圈数 | — |
| D | 总行驶时间 | mm:ss |
| E | 目标圈速 | mm:ss |
| F | 最优速度 | km/h |
| G | 燃料电池电压 | V |
| H | 超级电容电压 | V |
| I | 电机电压 | V |
| J | 燃料电池电流 | A |
| K | 超级电容电流 | A |
| L | 电机电流 | A |
| M | 自身消耗 | A |
| N | 燃料电池温度 | °C |
| O | 空气泵占空比 | % |
| P | 驾驶提示 | — |
| Q | 电芯电压差 | mV |
| R | 燃料电池累计能量 | Ws |
| S | 电机累计能量 | Ws |
| T | 燃料电池效率 | % |
| U | 系统效率 | % |

距离、电机功率（V × A）以及以 Wh 表示的能量均在手机端推算。

---

## 6. MQTT 主题与负载

所有消息均为 **QoS 0**（发后即忘）。空值/NaN 字段会被省略，以保持负载较小（约
100–200 字节）。

**`thaiger/{car}/telemetry`** —— 以配置频率发布（默认 5 Hz）：

```json
{"ts":1736517201234,"A":28.3,"B":25.0,"C":3,"D":"15:35","G":28.3,"N":53.4,"dist":0.42}
```

**`thaiger/{car}/gps`** —— 在有 GPS 定位时发布：

```json
{"ts":1736517201300,"lat":13.756331,"lon":100.501762,"alt":12.0,"spd":53.4,"acc":3.5,"hdg":270.0}
```

| 字段 | 含义 | 单位 |
|---|---|---|
| ts | Unix 时间戳 | ms |
| lat / lon | 纬度 / 经度 | ° |
| alt | 高度（若可用） | m |
| spd | GPS 速度（若可用） | km/h |
| acc | 水平精度（若可用） | m |
| hdg | 航向 / 方位（若可用） | ° |

---

## 7. 车辆配置档

| 配置档 | id | 最大电机功率 | 燃料电池温度上限 | 最低目标速度 |
|---|---|---|---|---|
| Thaiger 7 | `thaiger7` | 600 W | 70 °C | 25 km/h |
| Bengalo | `bengalo` | 900 W | 65 °C | 25 km/h |

阈值可在 **设置** 中按车覆盖。注意：*工程师仪表盘* 的地图轨迹使用独立的 **200 W** 功率
刻度（`POWER_MAX`），是为追踪显示而调，而非赛车的绝对峰值。

---

## 8. 故障排查

| 现象 | 原因 / 解决 |
|---|---|
| App 无法通过蓝牙连接 | 先确保模块已在安卓蓝牙设置中**配对**；检查其已通电；授予蓝牙 + 定位权限。可从选择对话框使用 **Pair Neu / 配对新设备**。 |
| 显示“Connected”却无数据 | 选错了设备，或控制器未发送。先用演示模式确认 App 正常，再重新检查模块。 |
| 工程师仪表盘始终不变绿 | **WebSocket URL** 错误（HiveMQ Cloud 必须是 `wss://…:8884/mqtt`，而非原生端口 `8883`）、凭据错误，或 broker 没有 WebSocket 监听端口。 |
| 仪表盘变绿但无遥测 | 仪表盘中的 **Car** 与手机的车辆 ID 不一致，或手机未启用转发。两端必须使用相同的 `thaiger7`/`bengalo`。 |
| 地图空白 | 没有互联网加载 OSM 瓦片，或尚无 GPS 定位。地图只有在收到定位后才会居中。 |
| GPS 轨迹乱跳 | 清理机制本应避免此问题；若你的设备只有非常粗糙的定位，请调高 `engineer_dashboard.html` 中的 `GPS_MAX_ACC`。**Filtered** 计数器显示有多少定位被剔除。 |
| GPS 每隔几秒才更新一次 | 确保手机有开阔的天空视野并已授予**定位**权限；在室内会回退到粗糙的网络定位。 |
| 未生成 CSV | 你必须完成一次有记录帧的行驶；若未捕获任何数据，*Export CSV* 按钮会提示 “No data to export”。 |

---

## 9. 调参与高级设置

**在手机上（设置 / MQTT 设置）：** 屏幕常亮、速度颜色、UI 刷新频率、振动/声音、每车阈值，
以及所有 broker/转发参数（见 A.7 / A.8）。

**在 `engineer_dashboard.html` 中**（脚本块顶部的常量）：

| 常量 | 默认 | 用途 |
|---|---|---|
| `POWER_MAX` | 200 | 映射为轨迹/图例满红的瓦数值 |
| `GPS_MAX_ACC` | 75 | 剔除精度差于此米数的 GPS 定位 |
| `GPS_MAX_SPEED` | 45 | m/s；隐含速度更快 ⇒ 视为瞬移 |
| `GPS_MIN_MOVE` | 1.5 | m；更小的移动视为静止抖动 |
| `GPS_SMOOTH` | 0.35 | EMA 平滑系数（0 = 不平滑，1 = 原始） |
| `GPS_MAX_REJECTS` | 4 | 连续剔除这么多次后重新同步 |
| `TRAIL_MAX` | 250 | 内存中保留的最大轨迹点数 |

---

## 10. 术语表

- **BLE** —— 低功耗蓝牙，赛车控制器到手机的无线链路。
- **MQTT** —— 一种轻量级的发布/订阅消息协议；broker 把消息从手机转发到工程师仪表盘。
- **Broker（代理服务器）** —— 双方都连接的 MQTT 服务器（例如 HiveMQ Cloud）。
- **TLS** —— broker 连接的加密。
- **WebSocket** —— 浏览器用于承载 MQTT 的传输方式（`ws://` / `wss://`）。
- **燃料电池（FC）** —— 赛车上的氢能源。
- **超级电容（Supercap）** —— 用于应对瞬时大功率的储能缓冲。
- **遥测帧** —— 来自赛车的一包测量数据。
- **轨迹（Trail）** —— 在地图上车后绘制的线，这里按电机功率着色。
